# yandex-direct-manager — Python-зависимости
# Установка: pip install -r requirements.txt
# Или: python install.py (с проверками)

# Таблицы для Direct Commander (ads.xlsx / keywords.xlsx);
# без openpyxl скрипт сам откатывается на CSV
openpyxl>=3.1

# Медиаплан в формате .docx
python-docx>=1.0

# Генерация визуалов (Шаг 8.5): OpenAI Images API, обработка/валидация
# картинок, HTTP-запросы к Replicate
openai>=1.40
Pillow>=10.0
requests>=2.31
